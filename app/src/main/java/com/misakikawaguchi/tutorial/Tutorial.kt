package com.misakikawaguchi.tutorial

// テスト対処クラス
class Calculator {
    // 掛け算
    fun multiply(x: Int, y: Int): Int {
        return x * y
    }

    // 割り算
    fun divide(x: Int, y: Int): Int {
        return x / y
    }
}